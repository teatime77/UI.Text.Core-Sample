<Page
    x:Class="Test.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Test"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="MainGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" BorderThickness="1" >
        <TextBlock x:Name="EditText" Margin="0,100,0,0"/>
        <Button x:Name="OverlappedButton" GotFocus="OverlappedButton_GotFocus" LostFocus="OverlappedButton_LostFocus" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Opacity="0" Click="OverlappedButton_Click" />
    </Grid>
</Page>
